#include<stdio.h>

main(){
	int a,b,c;
	printf(" Nhap 3 so a , b ,c can tim max : ");
	scanf("%d%d%d", &a, &b, &c);
	int max = 0;
	if(a > max);
	
	if(b> max)
		max = b;
	if(c > max)
		max = c;
		for ( ; ; )
{
   	printf("%d ", max);

   // place code here
}
}
