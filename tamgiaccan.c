#include<stdio.h>

int main(){
		int i, j, n, t =0 ;
		scanf("%d",&n);
	/*
	
* * * * *
 * * * *
  * * *
   * *
    *
		for(i = 1 ; i <= n ; ++i){
			if(t != 0) printf("\n");
			for(j = 1 ; j <= n ; ++j){
				printf(j >= i ? "* " : " "); 
			}
			t++;
		}
	
	
	
	*
   * *
  * * *
 * * * *
* * * * *

	for(i = 1 ; i <= n ; ++i){
			if(t != 0) printf("\n");
			for(j = 1 ; j <= n ; ++j){
				printf(j > n- i ? "* " : " "); 
			}
			t++;
		}
		
		
	*/
}

